import Teams from '../containers/Teams/Index'
import TeamsView from '../containers/Teams/TeamsView'

const routers = {
  Teams: { screen: Teams },
  TeamsView: {screen:TeamsView}
}

export default routers